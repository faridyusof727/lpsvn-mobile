import 'package:flutter/cupertino.dart';

class ScreenAnnouncement extends StatelessWidget {
  const ScreenAnnouncement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is announcement"),
    );
  }

}